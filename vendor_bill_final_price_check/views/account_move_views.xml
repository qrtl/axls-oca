<?xml version="1.0" ?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//group[@id='header_left_group']" position="inside">
                <field
                    name="confirm_final_price"
                    attrs="{'invisible': [('move_type', '!=', 'in_invoice')]}"
                    groups="account.group_account_manager"
                />
                <field name="is_change_price" invisible="1" />
            </xpath>
        </field>
    </record>
    <record id="action_move_in_invoice_type" model="ir.actions.act_window">
        <field name="name">Bills to Allow Confirm</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="view_id" ref="account.view_in_invoice_bill_tree" />
        <field name="search_view_id" ref="account.view_account_invoice_filter" />
        <field
            name="domain"
        >[('move_type', '=', 'in_invoice'),('confirm_final_price', '=', False),('is_change_price', '=', True)]</field>
        <field name="context">{'default_move_type': 'in_invoice'}</field>
    </record>
    <menuitem
        id="menu_action_move_in_invoice_type"
        action="action_move_in_invoice_type"
        parent="account.menu_finance_payables"
        groups="account.group_account_manager"
        sequence="2"
    />
</odoo>
