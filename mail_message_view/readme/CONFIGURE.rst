To get a button opening the messages of a record on its form view, set the
``mail_message_view.button_models`` system parameter to the comma separated list
of the wanted models, e.g. ``purchase.order,sale.order``.
