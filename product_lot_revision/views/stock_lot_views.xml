<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Stock Lot Form View Inheritance -->
    <record id="view_stock_lot_form_inherit" model="ir.ui.view">
        <field name="name">stock.lot.form.inherit</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='product_id']" position="after">
                <field
                    name="revision_id"
                    options="{'no_create': True}"
                    context="{'product_id': product_id}"
                />
                <field name="revision_number" />
                <button
                    name="action_view_revision"
                    type="object"
                    class="oe_link"
                    icon="fa-list"
                    attrs="{'invisible': [('revision_id', '=', False)]}"
                >
                    <span>View Revision</span>
                </button>
                <button
                    name="action_create_revision"
                    type="object"
                    class="oe_link"
                    icon="fa-plus"
                    attrs="{'invisible': [('product_id', '=', False)]}"
                >
                    <span>Create Revision</span>
                </button>
            </xpath>
        </field>
    </record>

    <!-- Stock Lot Tree View Inheritance -->
    <record id="view_stock_lot_tree_inherit" model="ir.ui.view">
        <field name="name">stock.lot.tree.inherit</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.view_production_lot_tree" />
        <field name="arch" type="xml">
            <field name="product_id" position="after">
                <field name="revision_id" optional="show" />
                <field name="revision_number" optional="show" />
            </field>
        </field>
    </record>

    <!-- Stock Lot Search View Inheritance -->
    <record id="view_stock_lot_search_inherit" model="ir.ui.view">
        <field name="name">stock.lot.search.inherit</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="stock.search_product_lot_filter" />
        <field name="arch" type="xml">
            <field name="product_id" position="after">
                <field name="revision_id" />
                <field name="revision_number" />
            </field>
            <filter name="group_by_product" position="after">
                <filter
                    string="Revision"
                    name="group_by_revision"
                    context="{'group_by': 'revision_id'}"
                />
            </filter>
        </field>
    </record>
</odoo>
